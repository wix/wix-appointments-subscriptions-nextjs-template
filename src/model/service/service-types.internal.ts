export enum ServiceType {
  INDIVIDUAL = 'INDIVIDUAL',
  GROUP = 'GROUP',
  COURSE = 'COURSE',
}

export enum OfferedAsType {
  ONLINE = 'ONE_TIME',
  OFFLINE = 'ONE_TIME',
  PRICING_PLAN = 'PRICING_PLAN',
}
